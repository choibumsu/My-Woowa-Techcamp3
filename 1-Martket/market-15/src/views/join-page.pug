doctype html
head
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title 배민사장님광장
  link(rel='stylesheet' href='/static/stylesheets/css/reset.css')
  link(rel='stylesheet' href='/static/stylesheets/css/base.css')
  link(rel='stylesheet' href='/static/stylesheets/css/signup.css')
  script(src='https://t1.daumcdn.net/mapjsapi/bundle/postcode/prod/postcode.v2.js')
main#signup-container
  .gray-bg
    .modal-wrapper
      .modal-box
        h2 인증번호를 발송했습니다.
        .content 휴대폰 SMS 발송된 인증번호를 확인해 주세요.
      .close 확인
  section.essential-form
    .logo-wrapper
      img.logo(src='/static/images/logo.png' alt='logo')
    .title 필수 정보를 입력해주세요.
    .input-wrapper
      input(type='text' name='id' data-type='$idInput' placeholder='아이디* (4~20자)')
      .error-message 예시 에러 메세지입니다.
      .allow-message 입력하신 아이디로 사용이 가능합니다.
    .input-wrapper

      input(type='password' name='password' data-type='$passwordInput' placeholder='비밀번호* (영문+숫자, 8~20자)')
      .error-message 예시 에러 메세지입니다.
    .input-wrapper
      input(type='password' name='passwordConfirm' data-type='$passwordConfirmInput' placeholder='비밀번호 확인')

      .error-message 예시 에러 메세지입니다.
    .input-wrapper.email-wrapper
      input.email(type='text' name='emailPrefix' data-type='$emailPrefixInput' placeholder='이메일 앞자리*')
      span.at @
      input.email.no-change(disabled='' name='emailSuffix' data-type='$emailSuffixInput' type='text' placeholder='이메일 뒷자리*')
      .error-message 예시 에러 메세지입니다.
    .input-wrapper
      select.selectEmail
        option(disabled='' selected='' value='') 이메일 선택
        option(value='naver.com') naver.com
        option(value='gmail.com') gmail.com
        option(value='self') 직접입력
    .input-wrapper
      input(type='text' name='name' data-type='$nameInput' placeholder='이름*')
      .error-message 예시 에러 메세지입니다.
    .input-wrapper.button-input-wrapper
      input.short(type='tel' name='phone' data-type='$phoneInput' placeholder='휴대폰*')
      button.input-button.phone-auth-btn(type='button') 인증받기
      .error-message 예시 에러 메세지입니다.
    .input-wrapper.button-input-wrapper.dp-none
      .time-input-wrapper
        input.short(type='tel' name='phoneAuth' data-type='$phoneAuthInput' placeholder='인증번호 6자리 입력')
        .time 02:00
      button.input-button.phone-auth-btn.active(type='button') 확인
      .error-message 예시 에러 메세지입니다.
    ul.auth-help-messages
      li · 인증번호 전송은 통신사에 따라 최대 1분까지 소요될 수 있습니다.
      li · 인증번호가 도착하지 않을 경우 '인증번호 재전송'을 눌러주세요.
  section.address-form
    .option-check-row
      input#address-check(type='checkbox')
      label.option-label(for='address-check') 선택 정보를 입력하시겠어요?
      .term-link 내용보기
    .input-wrapper.button-input-wrapper
      input.short(disabled='' type='number' name='postal' data-type='postalCode' placeholder='우편번호')
      button.input-button.address-search-btn(type='button') 주소찾기
      .error-message 예시 에러 메세지입니다.
    .input-wrapper
      input(disabled='' type='text' name='address' data-type='address' placeholder='주소')
      .error-message 예시 에러 메세지입니다.
    .input-wrapper
      input(disabled='' type='text' name='address-detail' data-type='addressDetail' placeholder='상세주소')
      .error-message 예시 에러 메세지입니다.
    .preview-wrapper.dp-none
      .address-label 지번
      .address-preview
        span.pre
        span.detail
  section.term-form
    .term-wrapper.all-term
      .checkbox-wrapper
        input#all(type='checkbox' name='all')
        label.term-title.strong(for='all') 전체 약관에 동의합니다
    .term-wrapper.essential-term
      .checkbox-wrapper
        input#essentail(type='checkbox' name='essential')
        label.term-title(for='essentail') 필수 약관에 동의합니다.
      .sub-wrapper.term-wrapper
        .term-link 배민사장님장 이용약관
        .term-link 개인정보 수집이용 동의
    .term-wrapper.optional-term
      .checkbox-wrapper
        input#optional(type='checkbox' name='optional')
        label.term-title(for='optional') 광고성 정보 수신 동의 (선택)
      p.sub-wrapper.description
        | 배민사장님광장 회원에게 제공하는 서비스의 광고성 정보를 수신합니다.
    .age-term-wrapper
      .term-content · 만 14세 이상 회원만 가입 가능합니다.
      .term-link 내용보기
  section.button-row
    .woowa-btn 가입완료
script(type='module' src='/static/javascripts/pages/JoinPage.js')

